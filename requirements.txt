# --- Required ---
PyYAML>=6.0          # read config.yaml / watchlist.yaml

# --- US quotes (TW market uses FinMind, no package needed) ---
yfinance>=0.2.40

# --- Visualization dashboard (app.py) ---
streamlit>=1.49        # width="stretch" API (取代已棄用的 use_container_width)
plotly>=5.18
pandas>=2.0

# --- Optional: prettier CLI output (falls back to plain text if absent) ---
rich>=13.0

# --- Optional: desktop notifications for `watch` (pick either one) ---
# win10toast>=0.9   # Windows
# plyer>=2.1        # cross-platform
